apiVersion: v1
kind: Service
metadata:
  name: {{ include "guacamole-webapp-gateway.fullname" . }}
  labels:
    {{- include "guacamole-webapp-gateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "guacamole-webapp-gateway.selectorLabels" . | nindent 4 }}
