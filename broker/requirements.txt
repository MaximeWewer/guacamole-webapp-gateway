# Web framework
flask
gunicorn

# Docker SDK
docker

# Kubernetes SDK (optional, only needed for Kubernetes backend)
kubernetes

# HTTP client
requests

# PostgreSQL
psycopg2-binary

# YAML config
pyyaml
